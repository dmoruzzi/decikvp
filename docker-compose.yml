services:
  kvp:
    image: ghcr.io/dmoruzzi/decikvp:latest
    ports:
      - "8080:8080"
    volumes:
      - kvp_data:/app
    environment:
      - API_KEY=${API_KEY:-}
    restart: unless-stopped

volumes:
  kvp_data:
